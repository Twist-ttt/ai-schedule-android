<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.ScheduleListActivity"
            android:label="@string/schedule_title" />

        <activity
            android:name=".ui.AiConfirmActivity"
            android:label="@string/confirm_title" />

        <activity
            android:name=".ui.DetailActivity"
            android:label="@string/detail_title" />

        <activity
            android:name=".ui.HistoryActivity"
            android:label="@string/history_title" />

        <activity
            android:name=".ui.SettingsActivity"
            android:label="@string/settings_title" />

        <!-- 日程数据 ContentProvider：以 URI 暴露 events 表（exported=true 便于演示与 adb 验证） -->
        <provider
            android:name=".provider.ScheduleProvider"
            android:authorities="com.qiu.aischedule.provider"
            android:exported="true" />

        <!-- AlarmManager 到点触发的提醒接收器 -->
        <receiver
            android:name=".notify.AlarmReceiver"
            android:exported="false" />
    </application>

</manifest>